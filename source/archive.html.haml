%h1 All Blog Posts
%ul.list
  - page_articles.each_with_index do |article, i|
    %li
      %span.date= article.date.strftime('%Y, %b %e')
      = link_to article.title, article, class: "internal-link"

